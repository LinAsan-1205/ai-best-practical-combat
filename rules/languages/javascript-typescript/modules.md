## 模块与导入导出规范（节 8）

本文件对应原始规范 `javascript-typescript-standards.md` 中的：

- `## 8. 模块`

目前完整内容仍在大文件中，你可以按需将该小节剪切/移动到这里。

推荐迁移步骤：

1. 将 `## 8. 模块` 开头到 `## 9. React 性能优化规范` 之前的所有内容复制到本文件。
2. 在大文件中用简短摘要替换原小节正文，并加一句：
   - “详细规则见 `rules/languages/javascript-typescript/modules.md`”。

