## JavaScript / TypeScript 语言选择（节 1）

本文件对应原始规范 `javascript-typescript-standards.md` 中的：

- `## 1. 语言选择`

目前完整内容仍在大文件中，你可以按需将该小节剪切/移动到这里。

推荐迁移步骤：

1. 将 `## 1. 语言选择` 开头到 `## 2. TypeScript 配置` 之前的所有内容复制到本文件。
2. 在大文件中用简短摘要替换原小节正文，并加一句：
   - “详细规则见 `rules/languages/javascript-typescript/language-choice.md`”。

