## ESLint 配置规范（节 12）

本文件对应原始规范 `javascript-typescript-standards.md` 中的：

- `## 12. ESLint 配置`

目前完整内容仍在大文件中，你可以按需将该小节剪切/移动到这里。

推荐迁移步骤：

1. 将 `## 12. ESLint 配置` 开头到 `## 13. 注释规范（参考 Google）` 之前的所有内容复制到本文件。
2. 在大文件中用简短摘要替换原小节正文，并加一句：
   - “详细规则见 `rules/languages/javascript-typescript/eslint.md`”。

