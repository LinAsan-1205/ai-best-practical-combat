## JavaScript / TypeScript 核心原则（节 0）

本文件对应原始规范 `javascript-typescript-standards.md` 中的：

- `## 0. 核心原则`

目前完整内容仍在大文件中，你可以按需将该小节剪切/移动到这里，或保持大文件作为“总览”，本文件作为拆分后的入口。

推荐后续迁移方式：

1. 将 `## 0. 核心原则` 及其下方内容复制到本文件。
2. 在大文件里，把原小节替换成一段简短摘要，并添加一句：
   - “详细规则见 `rules/languages/javascript-typescript/core-principles.md`”。

