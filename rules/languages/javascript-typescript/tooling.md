## 工具推荐与配套生态（节 16）

本文件对应原始规范 `javascript-typescript-standards.md` 中的：

- `## 16. 工具推荐`

目前完整内容仍在大文件中，你可以按需将该小节剪切/移动到这里。

推荐迁移步骤：

1. 将 `## 16. 工具推荐` 及其后内容复制到本文件（通常是整份文档的最后一节）。
2. 在大文件中用简短摘要替换原小节正文，并加一句：
   - “详细规则见 `rules/languages/javascript-typescript/tooling.md`”。

