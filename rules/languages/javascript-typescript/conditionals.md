## 条件表达式规范（节 11）

本文件对应原始规范 `javascript-typescript-standards.md` 中的：

- `## 11. 条件表达式规范`

目前完整内容仍在大文件中，你可以按需将该小节剪切/移动到这里。

推荐迁移步骤：

1. 将 `## 11. 条件表达式规范` 开头到 `## 12. ESLint 配置` 之前的所有内容复制到本文件。
2. 在大文件中用简短摘要替换原小节正文，并加一句：
   - “详细规则见 \`rules/languages/javascript-typescript/conditionals.md\`”。

