## 注释规范（参考 Google）（节 13）

本文件对应原始规范 `javascript-typescript-standards.md` 中的：

- `## 13. 注释规范（参考 Google）`

目前完整内容仍在大文件中，你可以按需将该小节剪切/移动到这里。

推荐迁移步骤：

1. 将 `## 13. 注释规范（参考 Google）` 开头到 `## 14. 代码审查清单` 之前的所有内容复制到本文件。
2. 在大文件中用简短摘要替换原小节正文，并加一句：
   - “详细规则见 `rules/languages/javascript-typescript/comments-google.md`”。

